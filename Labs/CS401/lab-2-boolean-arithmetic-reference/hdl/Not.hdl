/**
 * Not gate: out = not(in).
 * Built from a single Nand gate, tying both inputs together:
 * Not(in) = Nand(in, in).
 */
CHIP Not {
    IN in;
    OUT out;

    PARTS:
    Nand(a=in, b=in, out=out);
}
